export type Notepad = {
  id: number;
  title: string;
  subtitle: string;
  content: string;
  created_at: string;
};

export type User = {
  id: number;
  name: string;
  surname: string;
  age: number;
};
